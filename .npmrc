# Allow sqlite3 to run its install script (downloads native prebuild via
# prebuild-install). Without this, pnpm 10+ blocks all build scripts and
# `require('sqlite3')` fails at runtime with MODULE_NOT_FOUND.
onlyBuiltDependencies[]=sqlite3

# Don't hoist deps up to the workspace root (we're standalone now).
shared-workspace-lockfile=false
